pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.18
streamlit>=1.32

# Optional: only needed if you enable "Cloud AI" / "Local AI" narrative mode
# in the sidebar (engine.llm). The apps run fully without this installed.
openai>=1.30
